# Default database connection settings
server:
  address: "172.31.254.200"
  port: 11102

# Default application user credentials
user:
  name: "mbop_user"
  password: "mbop_pass"

# Default admin/root user credentials
admin:
  name: "root"
  password: "root"
